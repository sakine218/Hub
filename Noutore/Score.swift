import UIKit

class Score {
    let point: Int
    let username: String
    let time: String
    
    init(point: Int, username: String, time: String) {
        self.point = point
        self.username = username
        self.time = time
    }
}